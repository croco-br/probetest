namespace ExploringMars.Domain.Enums
{
    /// <summary>
    /// Cardinal Directions (North, West, East, South)
    /// </summary>
    public enum CardinalDirection { N, W, E, S }

    /// <summary>
    /// Probe commands (Turn Left, Turn Right, Move)
    /// </summary>
    public enum Command { L, R, M }
}